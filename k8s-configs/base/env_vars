############################################################
# Multi-region parameters
############################################################
IS_MULTI_CLUSTER=false

# Primary region name - must be a valid AWS region name
# Primary region should have the same value for REGION and PRIMARY_REGION
PRIMARY_REGION=us-west-2

# Tenant domain suffix for customer for region
# Primary region should have the same value for TENANT_DOMAIN and PRIMARY_TENANT_DOMAIN
PRIMARY_TENANT_DOMAIN=amehrotra-primary.ping-demo.com

# Optional comma-separated list of the tenant domain suffixes of secondary regions used for
# PingFederate adaptive clustering using the JGroups DNS_PING discovery protocol:
#
#   - Do NOT include the primary tenant domain in the list
#   - Do NOT include the CDE prefix in the domain name
#
# For example, if the primary domain is "tuna.us1.ping-preview.cloud", then set it
# to "tuna.eu1.ping-preview.cloud,tuna.au1.ping-preview.cloud" to specify secondary
# domains in regions eu1 and au1.
#
# NOTES:
#   - Only used in multi-cluster environments. If provided, PF will be configured with DNS_PING.
#   - It is recommended that SECONDARY_TENANT_DOMAINS be set apriori, even before the
#     secondary regions are created to avoid pod rollouts. DNS_PING will simply ignore
#     unreachable domains. If it is not set beforehand, then all servers in the PF
#     cluster (admin and engines) must be re-rolled to pick up the change.
SECONDARY_TENANT_DOMAINS=""

# Region-independent URL used for DNS failover/routing
GLOBAL_TENANT_DOMAIN=global.amehrotra-primary.ping-demo.com

############################################################
# S3 buckets
############################################################
# Ping-hosted common artifacts URL
PING_ARTIFACT_REPO_URL=https://ping-artifacts.s3-us-west-2.amazonaws.com

# Customer-specific log and backup URLs for region
PD_MONITOR_BUCKET_URL=ssm://amehrotra-primary/pcpt/service/storage/pd-monitor/uri
LOG_ARCHIVE_URL=ssm://amehrotra-primary/pcpt/service/storage/logs/uri
BACKUP_URL=ssm://amehrotra-primary/pcpt/service/storage/backup/uri
PGO_BACKUP_BUCKET_NAME=amehrotra-primary-pgo-bucket

############################################################
# Platform event queue and SSM variables
############################################################
PLATFORM_EVENT_QUEUE_NAME=amehrotra_primary_platform_event_queue.fifo
PLATFORM_EVENT_QUEUE_POLL_BATCH_SIZE=1
PLATFORM_EVENT_QUEUE_POLL_INTERVAL_SECONDS=2
CUSTOMER_SSM_PATH_PREFIX=/amehrotra-primary/pcpt/customer
CUSTOMER_SSO_SSM_PATH_PREFIX=/amehrotra-primary/pcpt/customer/sso
CUSTOMER_TLS_SSM_PATH_PREFIX=/amehrotra-primary/pcpt/customer/tls
SERVICE_SSM_PATH_PREFIX=/amehrotra-primary/pcpt/service
ARGOCD_CDE_ROLE_SSM_TEMPLATE=/amehrotra-primary/pcpt/config/k8s-config/accounts/{env}/argo/role/arn
ARGOCD_CDE_URL_SSM_TEMPLATE=/amehrotra-primary/pcpt/config/k8s-config/accounts/{env}/cluster/private-link/cname

############################################################
# Cluster state repo details
############################################################
# The known-hosts file to trust the cluster state repo server for git/ssh cloning
KNOWN_HOSTS_CLUSTER_STATE_REPO="|1|apW5X+RQpxJd9owENPIdmj0yCWg=|LwhRy4bWOTGtbyfu6lPHb3lnQK8= ssh-rsa "

CLUSTER_STATE_REPO_URL=ssh://APKA2IO25QZR5RKID5ET@git-codecommit.us-west-2.amazonaws.com/v1/repos/amehrotra-primary-cluster-state-repo
CLUSTER_STATE_REPO_BRANCH=dev

# The application reconciliation timeout in seconds for ArgoCD, i.e. how frequently ArgoCD should poll the
# cluster-state-repo for changes. To disable reconciliation set it to 0.
APP_RESYNC_SECONDS=60

############################################################
# Miscellaneous ping-cloud-base variables
############################################################
# Namespace where Ping apps are deployed
PING_CLOUD_NAMESPACE=ping-cloud

# The ping-cloud-base git URL and branch for base Kubernetes manifests
K8S_GIT_URL=https://github.com/reyuk07/ping-cloud-base.git
K8S_GIT_BRANCH=PDO-5995

# The name of the Docker image registry
ECR_REGISTRY_NAME=public.ecr.aws/r2h3l6e4

############################################################
# Server profile variables
############################################################
SERVER_PROFILE_URL=ssh://APKA2IO25QZR5RKID5ET@git-codecommit.us-west-2.amazonaws.com/v1/repos/amehrotra-primary-profile-repo
SERVER_PROFILE_BRANCH=${CLUSTER_STATE_REPO_BRANCH}

############################################################
# Environment-specific variables
############################################################
# Used by server profile hooks
ENVIRONMENT_TYPE=${ENV}

# Used by Kubernetes manifests
ENV=dev
KUSTOMIZE_BASE=test
ACCOUNT_TYPE=non-ga

############################################################
# Lets Encrypt server
############################################################
LETS_ENCRYPT_SERVER=https://acme-staging-v02.api.letsencrypt.org/directory

############################################################
# PingCommon environment variables
############################################################
USER_BASE_DN=dc=example,dc=com

# Multiple Backend IDs Base DNs
USER_BASE_DN_2=
USER_BASE_DN_3=
USER_BASE_DN_4=
USER_BASE_DN_5=

ECR_ENV=/dev

############################################################
# PingFederate variables for environment
############################################################
PF_PD_BIND_PORT=1389
PF_PD_BIND_PROTOCOL=ldap
PF_PD_BIND_USESSL=false

PF_MIN_HEAP=1536m
PF_MAX_HEAP=1536m
PF_MIN_YGEN=768m
PF_MAX_YGEN=768m

# This variable's value will make it onto the branding for the PF admin console and
# will include the name of the environment and the region where it's deployed.
PF_ADMIN_CONSOLE_ENVIRONMENT=${ENV}-${REGION}

############################################################
# PingAccess variables for environment
############################################################
PA_WAS_MIN_HEAP=1024m
PA_WAS_MAX_HEAP=1024m
PA_WAS_MIN_YGEN=512m
PA_WAS_MAX_YGEN=512m
PA_WAS_GCOPTION=-XX:+UseParallelGC

PA_MIN_HEAP=1024m
PA_MAX_HEAP=1024m
PA_MIN_YGEN=512m
PA_MAX_YGEN=512m
PA_GCOPTION=-XX:+UseParallelGC

############################################################
# DevOps License environment variables
############################################################
PING_IDENTITY_DEVOPS_USER=amehrotra@pingidentity.com
PING_IDENTITY_DEVOPS_KEY=e94a63f3-6d8c-1c0f-848f-2aa79ec482c0

############################################################
# Ping application image tags
############################################################
PINGACCESS_IMAGE_TAG=v2.1-release-branch-latest
PINGACCESS_WAS_IMAGE_TAG=v2.1-release-branch-latest
PINGFEDERATE_IMAGE_TAG=v2.1-release-branch-latest
PINGDELEGATOR_IMAGE_TAG=v2.1-release-branch-latest
PINGCENTRAL_IMAGE_TAG=v2.1-release-branch-latest
PINGDATASYNC_IMAGE_TAG=v2.1-release-branch-latest

############################################################
# Ping solution image tags
############################################################
BOOTSTRAP_IMAGE_TAG=v2.1-release-branch-latest
P14C_INTEGRATION_IMAGE_TAG=v2.1-release-branch-latest

############################################################
# Ping observability image tags
############################################################
OS_BOOTSTRAP_IMAGE_TAG=v2.1-release-branch-latest
LOGSTASH_IMAGE_TAG=v2.1-release-branch-latest

############################################################
# Cluster name variables
############################################################
CLUSTER_NAME=amehrotra-primary
CLUSTER_NAME_LC=amehrotra-primary

############################################################
# Primary DNS zone
############################################################
PRIMARY_DNS_ZONE=${PRIMARY_TENANT_DOMAIN}

############################################################
# Public hostnames
############################################################
# Ping admin configuration required for admin access and clustering
PF_ADMIN_PUBLIC_HOSTNAME=pingfederate-admin.${PRIMARY_DNS_ZONE}
PF_ADMIN_API_PUBLIC_HOSTNAME=pingfederate-admin-api.${PRIMARY_DNS_ZONE}
PA_ADMIN_PUBLIC_HOSTNAME=pingaccess-admin.${PRIMARY_DNS_ZONE}
PA_ADMIN_API_PUBLIC_HOSTNAME=pingaccess-admin-api.${PRIMARY_DNS_ZONE}
PA_WAS_ADMIN_PUBLIC_HOSTNAME=pingaccess-was-admin.${PRIMARY_DNS_ZONE}
PC_ADMIN_PUBLIC_HOSTNAME=pingcentral-admin.${PRIMARY_DNS_ZONE}
PC_ADMIN_PRIVATE_SITE_HOSTNAME=pingcentral.${PRIMARY_TENANT_DOMAIN}

PD_CLUSTER_PUBLIC_HOSTNAME=pingdirectory-cluster.${PRIMARY_DNS_ZONE}
PF_CLUSTER_PUBLIC_HOSTNAME=pingfederate-cluster.${PRIMARY_DNS_ZONE}
PA_CLUSTER_PUBLIC_HOSTNAME=pingaccess-cluster.${PRIMARY_DNS_ZONE}
PA_WAS_CLUSTER_PUBLIC_HOSTNAME=pingaccess-was-cluster.${PRIMARY_DNS_ZONE}

# Ping engine hostname variables
PD_PUBLIC_HOSTNAME=pingdirectory-admin.${DNS_ZONE}
PD_HTTP_PUBLIC_HOSTNAME=pingdirectory.${DNS_ZONE}
PD_DELEGATOR_PUBLIC_HOSTNAME=pingdelegator.${DNS_ZONE}
PD_DELEGATOR_PUBLIC_PORT=443
PF_ENGINE_PUBLIC_HOSTNAME=pingfederate.${DNS_ZONE}
PA_ENGINE_PUBLIC_HOSTNAME=pingaccess.${DNS_ZONE}
PA_WAS_ENGINE_PUBLIC_HOSTNAME=pingaccess-was.${DNS_ZONE}

PROMETHEUS_PUBLIC_HOSTNAME=prometheus.${DNS_ZONE}
GRAFANA_PUBLIC_HOSTNAME=monitoring.${DNS_ZONE}
OSD_PUBLIC_HOSTNAME=logs.${DNS_ZONE}
ARGOCD_PUBLIC_HOSTNAME=argocd.${DNS_ZONE}
SELF_SERVICE_PUBLIC_HOSTNAME=self-service-api.${PRIMARY_DNS_ZONE}

HEALTHCHECK_HTTPBIN_PA_WAS_PUBLIC_HOSTNAME=healthcheck-httpbin-pa-was.${PRIMARY_DNS_ZONE}
HEALTHCHECK_HTTPBIN_PA_PUBLIC_HOSTNAME=healthcheck-httpbin-pa.${PRIMARY_DNS_ZONE}

# Retention time for Prometheus. Could be set in years(y), weeks(w), days(d), hours(h), minutes(m) or seconds(s)
PROMETHEUS_RETENTION_TIME=15d

# Commands and schedules to obtain users count. Values should be in single quotes. Schedules should be in cron format. Command should return only numeric value(int\float)
PROMETHEUS_USER_COUNT_1_COMMAND='echo 0'
PROMETHEUS_USER_COUNT_1_SCHEDULE='* * * * *'
PROMETHEUS_USER_COUNT_2_COMMAND='echo 0'
PROMETHEUS_USER_COUNT_2_SCHEDULE='* * * * *'
PROMETHEUS_USER_COUNT_3_COMMAND='echo 0'
PROMETHEUS_USER_COUNT_3_SCHEDULE='* * * * *'
PROMETHEUS_USER_COUNT_4_COMMAND='echo 0'
PROMETHEUS_USER_COUNT_4_SCHEDULE='* * * * *'

# Variable to have ping-cloud namespace in prometheus-job-exporter
PROMETHEUS_PING_NAMESPACE=ping-cloud

#### FEATURE FLAGS #####################################################################################################
# Set to true to enable, most are disabled by default
# Please follow runbook instructions for the given feature flag to enable/disable

# !!! Provisioning for PingFederate is not yet available for multi-region customers !!!
# Runbook: https://confluence.pingidentity.com/display/PingCloud/How+to+Enable+and+Configure+PingFederate+Provisioning
PF_PROVISIONING_ENABLED=false

RADIUS_PROXY_ENABLED=false
# List of ping apps(pingaccess,pingaccess-was,pingdelegator,pingfederate) for which you can enable external ingress(the values are ping app names)Examplelist:(pingaccess pingaccess-was pingfederate)
# Note pingdirectory external ingress is now controlled through the value `extIngressEnabled' in the 'p1as-pingdirectory/<region>/values.yaml' file.
EXTERNAL_INGRESS_ENABLED=""

# Runbook: https://pingidentity.atlassian.net/wiki/spaces/PDA/pages/427688292/How+to+Enable+and+Disable+Healthchecks+-+1.19.0
HEALTHCHECKS_ENABLED=false

CUSTOMER_PINGONE_ENABLED=false
########################################################################################################################

# For ArgoCD-status slack notifications (notifications.argoproj.io/subscribe.on-sync-failed.slack)
SLACK_CHANNEL=nowhere

# Dashboards repo variables
DASH_REPO_URL=https://github.com/pingidentity/ping-cloud-dashboards
DASH_REPO_BRANCH=v2.1-dev-branch

# Healthcheck variables
CLUSTER_HEALTHCHECK_SCHEDULE="* * * * *"
PINGACCESS_HEALTHCHECK_SCHEDULE="* * * * *"
PINGACCESS_WAS_HEALTHCHECK_SCHEDULE="* * * * *"
PINGDIRECTORY_HEALTHCHECK_SCHEDULE="* * * * *"
PINGFEDERATE_HEALTHCHECK_SCHEDULE="* * * * *"


# cert-manager variables
# The maximum duration allowed by Let's Encrypt for certificate rotation is 90 days (equivalent to 2160 hours).
# See here for more details: https://letsencrypt.org/docs/faq/#what-is-the-lifetime-for-let-s-encrypt-certificates-for-how-long-are-they-valid
# But it allows the certificate to be renewed before the duration.
# Renewing before 30 days (aka 720 hours) of expiration is suggested by let's encrypt
# Meaning if cert expires in 90 days we will rotate and renew in 60 days
# NOTE: Format must exactly match Argo's expectations until https://github.com/argoproj/argo-cd/pull/16587 is fixed
# Format example: 720h0m0s - check Argo sync if not sure after changing this value to make sure there is no diff.
CERT_RENEW_BEFORE=720h0m0s

# Variable to set the default cluster resource uptime/working hours
DEFAULT_CLUSTER_UPTIME="Mon-Fri 11:00-23:00 Asia/Kolkata"

IS_BELUGA_ENV=true
